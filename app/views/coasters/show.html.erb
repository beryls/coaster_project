<! shows a coaster and that coaster's park >
<h1><%= @coaster.name %></h1>
<h2>Park: <%= @park.name %></h2>

<!-- if a user is logged in, creates check box that is checked if user has ridden coaster
turn this into a form to add or remove coaster from user's coaster list -->
<% if @user %>
  <%= label_tag "Have you ridden this coaster?" %>
    <%= check_box_tag "user.coasters", @coaster.id, @user.coasters.include?(@coaster) %>
<% end %>

<! lists coaster's attributes >
<ul>
  <li>Type: <%= @coaster.material %></li>
  <li>Height: <%= @coaster.height %></li>
  <li>Length: <%= @coaster.length %></li>
  <li>Speed: <%= @coaster.speed %></li>
  <li>Inversions: <%= @coaster.inversions %></li>
</ul>

<%= render 'links' %>

<! API for Google Maps - set up to show the coaster's park >
<%= gmaps4rails(@json) %><br/>